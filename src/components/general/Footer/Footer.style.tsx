import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    padding: 15,
    paddingTop: 5,
    borderTopWidth: 1,
    borderColor: '#B3B3B3',
  },
});
